package com.csci448.fpmobileapp.data.repos

import com.csci448.fpmobileapp.data.Saurus

/**
 * holds the users' Sauruses
 *
 * not sure if this is even needed if the user only has the one
 */
object SaurusRepo {
    val mySaurus = Saurus("_TESTSAURUS_")
}